import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  search: {
    height: 40,
    margin: 12,
    marginTop: 70,
    borderRadius: 10,
    borderWidth: 0.5,
    padding: 10,
    color: 'black',
  },
  input: {
    height: 40,
    margin: 12,
    borderWidth: 0.5,
    borderRadius: 10,
    padding: 10,
    color: 'black',
  },
});

export default styles;
